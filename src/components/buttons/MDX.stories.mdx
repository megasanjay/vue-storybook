export default {};

import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { NButton } from "naive-ui";

<Meta
  title="Example/MDX Test"
  component={NButton}
  argTypes={{
    type: {
      control: { type: "select" },
      options: [
        "default",
        "primary",
        "tertiary",
        "info",
        "success",
        "warning",
        "error",
      ],
    },
  }}
/>

export const TypeTemplate = (args) => ({
  components: { NButton },
  setup() {
    return { args };
  },
  template: '<n-button v-bind="args">Button</n-button>',
});

<Story
  name="Type"
  args={{
    type: "default",
  }}
>
  {TypeTemplate.bind({})}
</Story>
