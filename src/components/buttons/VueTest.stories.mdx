import { Meta, Preview, Story } from "@storybook/addon-docs";
import { NCard } from "naive-ui";

<Meta title="Example/VueTest" component={NCard} />

# Badge

Let's define a story for our `Badge` component:

export const TitleTemplate = (args) => ({
  components: { NCard },
  setup() {
    return { args };
  },
  template: '<n-card v-bind="args">Card Content</n-card>',
});

<Story name="Title" args={{ title: "Card" }}>
  {TitleTemplate.bind({})}
</Story>
